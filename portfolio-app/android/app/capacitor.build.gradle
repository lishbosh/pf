// DO NOT EDIT THIS FILE! IT IS GENERATED EACH TIME "capacitor update" IS RUN

android {
  compileOptions {
      sourceCompatibility JavaVersion.VERSION_21
      targetCompatibility JavaVersion.VERSION_21
  }
}

// Robust approach to include cordova.variables.gradle
// First try the standard project reference
try {
    if (findProject(':capacitor-cordova-android-plugins')) {
        apply from: project(':capacitor-cordova-android-plugins').file('cordova.variables.gradle')
    } else {
        // If that fails, try direct file reference with multiple fallbacks
        def cordovaVariablesFile = new File(projectDir, "../capacitor-cordova-android-plugins/cordova.variables.gradle")
        if (cordovaVariablesFile.exists()) {
            apply from: cordovaVariablesFile
        } else {
            // Try alternative path
            cordovaVariablesFile = new File(rootDir, "capacitor-cordova-android-plugins/cordova.variables.gradle")
            if (cordovaVariablesFile.exists()) {
                apply from: cordovaVariablesFile
            } else {
                // Log warning but don't fail the build
                logger.warn("Could not find cordova.variables.gradle file at expected locations")
            }
        }
    }
} catch (Exception e) {
    logger.warn("Failed to apply cordova.variables.gradle: ${e.message}")
}

dependencies {
}

if (hasProperty('postBuildExtras')) {
  postBuildExtras()
}